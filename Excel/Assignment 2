1. What does the dollar($) sign do?
   Dollar sign can denote a currency format, but it has another common use is indicating absolute cell references in formulas.

2. How to Change the Reference from Relative to Absolute (or Mixed)?
   Press F4 to switch between the reference types.

3. Explain the order of operations in excel?
   If a formula contains both a multiplication and division operator then Excel evaluates the operators from left to right.

4. What, according to you, are the top 5 functions in excel and write a basic syntax for any of two?
   SUM Function, TEXT Function, VLOOKUP Function, AVERAGE Function, CONCATENATE Function
   SUM function -  =SUM (number1, number2, ....n)
   VLOOKUP Function - =VLOOKUP (lookup_value, table_array, col_index_num, *range_lookup*)
   
5. When would you use the subtotal function?
   SUBTOTAL function is designed for columns of data, or vertical ranges. It is not designed for rows of data, or horizontal ranges.

6. What is the syntax of the vlookup function? Explain the terms in it?
   VLOOKUP Function - =VLOOKUP (lookup_value, table_array, col_index_num, *range_lookup*)
   Explanation - (lookup_value = It is the data you want to find)
                 (table_array = It is the data column where you want to limit your search)
                 (col_index_num = It is the column number within the table that you want to return a value from)
                 (range_lookup = It is an optional argument that allows you to search for the exact match of your lookup value without sorting the table)
